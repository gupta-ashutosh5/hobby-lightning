{#
/**
 * @file user_quiz_score.html.twig
 * Default theme implementation to present User quiz score data.
 *
 * This template is used when viewing User quiz score pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_user_quiz_score()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('user_quiz_score') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
